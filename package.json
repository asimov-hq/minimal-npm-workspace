{
  "name": "@asimov/minimal-workspace",
  "private": true,
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -b && npm run --workspace @asimov/minimal-web build",
    "build:packages": "npm run --workspaces --if-present build",
    "build:ts": "tsc -b",
    "typecheck": "tsc -b",
    "typecheck:packages": "npm run --workspaces --if-present typecheck",
    "clean": "npm run --workspaces --if-present clean",
    "dev:ts": "tsc -b -w tsconfig.dev.json",
    "dev:app": "concurrently -k -s first -n server,web \"npm run --workspace @asimov/minimal-server dev\" \"npm run dev:web\"",
    "dev:server": "npm run --workspace @asimov/minimal-server dev",
    "dev:web": "npm run --workspace @asimov/minimal-web dev",
    "dev:cli": "npm run --workspace @asimov/minimal-cli dev",
    "start:server": "npm run --workspace @asimov/minimal-server start",
    "start:web": "npm run --workspace @asimov/minimal-web preview",
    "start:cli": "npm run --workspace @asimov/minimal-cli start",
    "lint": "eslint . --cache",
    "test": "npm run --workspaces --if-present test",
    "smoke:copy": "bash scripts/copy-smoke.sh",
    "test:e2e": "node scripts/e2e-browser.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/node": "^25.0.8",
    "concurrently": "^9.2.0",
    "eslint": "^9.39.2",
    "playwright": "^1.61.1",
    "tsx": "^4.8.1",
    "typescript": "^5.8.0",
    "typescript-eslint": "^8.53.0"
  }
}
